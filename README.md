# 🧩 Figma → SCSS Extractor

Скрипт призначений для автоматичного оновлення файлу **SCSS-змінних** (`roots.scss`) на основі актуальних стилів із Figma. Він отримує **кольори**, **шрифти** та **тіні** з вибраного Figma-файлу або фрейму й оновлює лише ті змінні, які вже існують у вашому файлі.

---

## ⚙️ Встановлення

1. Клонуйте репозиторій:

   ```bash
   git clone https://github.com/Dmytro332116/Figma.git
   cd Figma
   ```

2. Встановіть залежності:

   ```bash
   npm install
   ```

3. Створіть файл `.env` у корені проєкту та додайте свій **Figma API Token**:

   ```bash
   FIGMA_TOKEN=your_personal_access_token
   ```

   🔗 Отримати токен можна тут: [Figma API Tokens](https://www.figma.com/developers/api#access-tokens)

---

## 🚀 Як користуватись

Запустіть команду:

```bash
node index.js
```

Далі відповідайте на запити в терміналі:

1. **Вкажіть шлях до SCSS файлу**
   Наприклад:

   ```
   dist/roots.scss
   ```

2. **Вставте посилання на Figma-файл або конкретний фрейм**, наприклад:

   ```
   https://www.figma.com/design/XXXXXXXXXXXX/MyProject?node-id=1234-5678
   ```

3. Скрипт автоматично:

   * Отримає `file_id` та `node_id` з посилання.
   * Завантажить локальні стилі (**Colors**, **Text**, **Effects**) або стилі конкретного фрейму.
   * Знайде збіги між назвами змінних у SCSS та назвами стилів у Figma.
   * Оновить лише ті значення, які вже є у файлі.
   * Збереже формат (`#HEX`, `rgba`, `#{rem()}`, `#{remD()}`).
   * Створить резервну копію `roots.scss.bak` перед оновленням.

---

## 💡 Приклад результату

**До:**

```scss
:root {
  --primary--500: #000000;
  --secondary--500: #cccccc;
}
```

**Після:**

```scss
:root {
  --primary--500: #26BCF7;
  --secondary--500: #FD6283;
}
```

Скрипт не додає нові змінні та не змінює структуру файлу — лише оновлює значення.

---

## 🧠 Додаткова інформація

* Якщо у Figma немає Local Styles — відкрий увесь файл, а не окремий фрейм.
* Якщо стилі є тільки у фреймі, вставляй **посилання на фрейм**, а не на проєкт.
* Формат шрифтів зберігається (`#{rem()}` або `#{remD()}`) як у вихідному SCSS.
* Створюється резервна копія перед будь-яким оновленням.

---

## 🔍 Корисні команди

Швидкий запуск:

```bash
npm run update-scss
```

(попередньо додайте у `package.json`):

```json
"scripts": {
  "update-scss": "node index.js"
}
```

---

## 👨‍💻 Автор

**Dmytro**
GitHub: [@Dmytro332116](https://github.com/Dmytro332116)

---

## 📄 Ліцензія

MIT License © 2025 Dmytro332116
