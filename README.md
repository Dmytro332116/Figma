# 📘 Figma Assets Automation — Інструкція для Верстальників

Повноцінний автоматизований інструмент для експорту **шрифтів**, **іконок**, **кольорів**, **тіней**, **розмірів тексту** та автоматичного оновлення **SCSS** напряму з Figma.

Це документація для вашої команди верстальників — щоб кожен легко міг встановити, налаштувати та користуватися інструментом.

---

# 🚀 Основні можливості

## ✔️ Автоматичне завантаження шрифтів

* Підтримує **Google Fonts**
* Підтримує **будь-які інші шрифти** через WebFontsHelper
* Автоматично розкладає шрифти по структурі:

```
dist/assets/fonts/Rubik/Regular/Rubik-Regular.woff2
dist/assets/fonts/Rubik/Bold/Rubik-Bold.woff2
```

* Визначає вагу, italic, назви варіантів
* Показує прогрес у відсотках
* Генерує список шрифтів, які потрібно додати вручну

---

## ✔️ Автоматичний експорт іконок

* Підтримка SVG та PNG
* Оптимізований експорт з правильною назвою файлів
* Авто-таблиця результатів для зручної перевірки
* Зберігає іконки у `dist/assets/icons/`

---

## ✔️ Оновлення SCSS змінних

* Екстрактить з Figma:

  * кольори
  * тіні
  * текстові розміри
* Автоматично оновлює `roots.scss`
* Створює резервну копію `roots.scss.bak`

---

## ✔️ ZIP архів після кожної дії

Створює архів:

```
dist/export_Figma.zip
```

Можна одразу передавати іншому розробнику.

---

# 🛠 Встановлення (1 раз)

## 1️⃣ Встановити Node.js LTS

Скачати: [https://nodejs.org/](https://nodejs.org/)

Перевірити:

```
node -v
npm -v
```

---

## 2️⃣ Клонувати репозиторій

```
git clone https://github.com/Dmytro332116/Figma.git
cd Figma
```

---

## 3️⃣ Встановити залежності

```
npm install
```

---

## 4️⃣ Створити `.env`

Створіть файл:

```
.env
```

І додайте в нього:

```
FIGMA_TOKEN=ваш_токен
WEBFONTS_HELPER_URL=https://gwfh.mranftl.com/fonts
```

Отримати токен: [https://www.figma.com/developers/api](https://www.figma.com/developers/api)

### Для зручності доступний шаблон:

```
cp .env.example .env
```

---

# ▶️ Як користуватися

Запустіть інструмент:

```
node index.js
```

Зʼявиться меню:

```
1️⃣ Оновити SCSS
2️⃣ Завантажити шрифти
3️⃣ Експорт іконок
```

Далі вводите **URL фрейму з Figma**, приклад:

```
https://www.figma.com/design/...?...node-id=1234-5678
```

---

# 📁 Структура результатів

Після кожного запуску у вас буде:

```
dist/
  assets/
    fonts/
      Rubik/...
      MacPaw Fixel/...
      manual/fonts.txt
    icons/
  export_Figma.zip
```

---

# 🧩 Як працює система (простими словами)

### 1. Ти вводиш лінк на фрейм у Figma

Інструмент отримує повну інформацію про:

* текстові стилі
* шрифти
* кольори
* тіні
* іконки

### 2. Інструмент аналізує всі шрифти

* Визначає сімʼї
* Розпізнає ваги, italic
* Звіряє у Google Fonts
* Якщо немає — бере з WebFontsHelper

### 3. Все красиво складається у папки

```
fonts/Rubik/Regular/Rubik-Regular.woff2
fonts/Rubik/Bold/Rubik-Bold.woff2
```

### 4. Іконки експортуються

Імена очищуються → формат нормалізується → файли кладуться у папку.

### 5. SCSS автоматично оновлюється

Кольори форматуються у змінні:

```
--color-primary: #ff5533;
--shadow-lg: 0 4px 16px rgba(0,0,0,.2);
```

### 6. Генерується ZIP

Його можна відправити іншому верстальнику або завантажити на сервер.

---

# ℹ️ Нова логіка заміни SCSS за slug із Figma

Головна помилка попередньої версії — **оновлення змінних «по порядку»**. Кольори, тіні та розміри тексту бралися з наборів і підставлялися у SCSS за індексом, тому `--greyscale--900` міг випадково отримати значення від `--primary--500`, а типографічні токени взагалі перетиралися випадковими розмірами. Тепер усе працює так:

1. Під час обходу фрейму інструмент читає **повний шлях вузла**: наприклад `Primary / 500` або `Desktop / Headline 1`.
2. Шлях перетворюється у **slug** (`primary-500`, `desktop-headline-1`). Для кожного вузла зберігається кілька варіантів slug (повна послідовність і її суфікси), щоб покрити різні неймінги у SCSS.
3. Кожен колір, тінь, font-size і line-height зберігаються у мапах `slug → значення`. Ми також тягнемо line-height і font-size окремо, аби токени типографії не плутались.
4. Під час оновлення SCSS кожне ім'я змінної теж перетворюється на slug (`--primary--500` → `primary-500`). Якщо slug збігається — оновлюємо **тільки цю змінну**. Немає збігів — пропускаємо без будь-яких зсувів.

Завдяки цьому можна гарантовано отримувати ті самі кольори, font-size, line-height і тіні, які знаходяться у Figma, незалежно від позиції елемента у макеті.

### 🔁 Як ми розв'язали конфлікт зі стилями

Коли стилі у Figma мають довгі назви (`Colors / Primary / Base / 100`), а в SCSS використовується коротка версія (`--primary--100`), попередня реалізація не знаходила збіг і підставляла «піпеткові» значення. Через це на GitHub зʼявлявся конфлікт саме у `README.md` та `index.js`, адже доводилося вручну правити значення тільки для `500`, а решта градацій залишалися без змін. Тепер генератор slugів створює **усі послідовні комбінації сегментів** (наприклад, `colors-primary`, `primary-base`, `base-100`, `colors-primary-base-100`) і додатково додає кожну **парну** комбінацію (наприклад, `primary-100`, `colors-100`). Будь-яка змінна з таким slug отримує точно той самий токен, що й стиль у Figma. У результаті всі відтінки (`100`, `200`, `900` тощо) оновлюються автоматично, а «піпетка» більше не використовується.

## 🎯 Чому кольори тепер зчитуються 1:1

Раніше експортер ігнорував **накопичену прозорість** і читав кольори «піпеткою» з будь-якого вузла у фреймі. Через це значення могли приходити лише для одного з відтінків (наприклад, 500), а всі решта залишалися без змін. Оновлений пайплайн працює у дві стадії:

1. Під час обходу дерева передається **ланцюг opacity** (`parentOpacity * node.opacity`).
2. Кольори тепер додатково підтверджуються **офіційними стилями Figma**: ми запитуємо `/files/{key}/styles`, дістаємо всі Paint/Text/Effect стилі, будуємо slug із `Primary / 500`, `Greyscale / 900` тощо і підставляємо значення в SCSS тільки коли назва стилю збігається зі slug змінної.
3. Для кожного fill/gradient/тіні множимо `nodeOpacity * paintOpacity * colorAlpha`, а для стилів Figma беремо точне значення, яке задано дизайнером — жодних випадкових «наступних кольорів».

У результаті палітра та типографічні токени тепер не тільки знаходять **свою** змінну завдяки slug, а й беруть дані строго з того ж стилю, який використано у Figma. Жодних «піпеток» і випадкових індексів.

### 🎛️ Правий стовпець Variables тепер теж під контролем

У Figma праворуч може бути не лише список стилів, а й **Variables** з усіма кольорами (`Color grey / 900`, `Support / 400` тощо). Раніше інструмент їх ігнорував, тому більшість відтінків узагалі не потрапляли до SCSS. Тепер логіка така:

1. Інструмент опитує `GET /files/{key}/variables/local` та `.../variables/published`, тож бачить усі локальні й підключені бібліотечні колекції.
2. Для кожної змінної беремо значення з дефолтного режиму колекції (Light/Dark) і розгортаємо alias-ланцюжки, якщо одна змінна посилається на іншу.
3. Імена змінних розрізаються на сегменти (`Color grey / 900` → `color-grey-900`), і за тим самим slug автоматично оновлюються SCSS-змінні `--color-grey--900`, `--support--400` тощо.
4. Якщо у Variables лежать **числові токени** (font-size, line-height), вони перетворюються на ті ж `#{rem(...)}` або unitless значення, що й під час обходу фрейму, а тіньові `STRING`-токени записуються без змін. Тобто правий стовпець може містити не лише кольори, а й повну типографію/тіні — все це тепер летить у SCSS за своїм slug.

Таким чином у палітру потрапляє **весь список з правої колонки Figma**, а не лише кольори, що випадково трапилися у фреймі.

---

# ⚠️ Важливо знати

* `.env` **не пушиться** у GitHub
* Не можна поширювати FIGMA_TOKEN
* Усі результати генеруються локально

---

# 🧃 Поради для верстальників

## ✔ Якщо шрифт не знайшовся

Перевірити:

* чи правильно вказана сімʼя у Figma
* чи існує шрифт в Google Fonts
* якщо він кастомний — додасться у `manual/fonts.txt`

## ✔ Якщо іконка експортувалась порожня

* перевірити чи іконка — Vector, а не Component з маскою

## ✔ Якщо SCSS не оновився

* перевірити чи фрейм містить стилізовані елементи

---

# 💬 Підтримка

Якщо щось не працює — пишіть у командний чат.
Цей README створено спеціально для спрощення роботи команди.

Успіхів у розробці! 🚀
